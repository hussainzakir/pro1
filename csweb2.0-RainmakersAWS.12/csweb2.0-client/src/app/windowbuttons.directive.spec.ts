import { WindowbuttonsDirective } from './windowbuttons.directive';

describe('WindowbuttonsDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowbuttonsDirective();
    expect(directive).toBeTruthy();
  });
});
